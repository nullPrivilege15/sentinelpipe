services:
  web:
    build: ./web
    container_name: vulnerable-llm-web
    ports:
      - "5000:5000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./web:/app
    restart: unless-stopped